fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2
pydantic==2.13.4
motor==3.3.1
pymongo==4.6.3
httpx==0.28.1
aiofiles==25.1.0
python-dotenv==1.2.2
python-multipart==0.0.32
APScheduler==3.11.3

# --- Optional: AI copy generation + Emergent object storage ---
# The `emergentintegrations` package is NOT on PyPI. If you have an Emergent
# account/key and want the /api/ai/generate endpoint to work, install it with:
#   pip install emergentintegrations --extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
# Without it the app runs fine; the AI endpoint returns 503.

# --- Dev/test only ---
pytest==9.1.1
pytest-xdist==3.8.0
requests==2.34.2
