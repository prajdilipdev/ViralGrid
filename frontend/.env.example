# URL of the backend API (no trailing slash). Baked in at build time.
# Local dev:  http://localhost:8000
# Render:     https://<your-backend-service>.onrender.com
REACT_APP_BACKEND_URL=http://localhost:8000
